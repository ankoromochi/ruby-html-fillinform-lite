require 'spec_helper'

describe Html::Fillinform::Lite do
  it 'has a version number' do
    expect(Html::Fillinform::Lite::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
